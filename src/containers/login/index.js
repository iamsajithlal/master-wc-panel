import React from "react";
import useStyles from "./style";

const Login = () =>{
    const classes = useStyles()
    return(
       <div className={classes.root}>Helloo</div>
    )
}

export default Login;